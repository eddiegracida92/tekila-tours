# No correr `pnpm install` automáticamente antes de cada script.
# Evita que `pnpm db:push`/`db:seed` fallen por un install colateral
# (paquetes ajenos inyectados en node_modules por herramientas externas).
verify-deps-before-run=false
